server:
  port: "8080"
  mode: "debug"  # "debug" or "release"

database:
  host: "localhost"
  port: "5432"
  user: "betterlife"
  password: "betterlife_dev"
  dbname: "betterlife"
  sslmode: "disable"
